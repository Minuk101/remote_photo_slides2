{
  "name": "remote-photo-slides2",
  "version": "1.0.0",
  "private": true,
  "description": "Local-network photo slideshow with visit-aware timeline locations",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "analyze": "node analyze.js",
    "check": "node --check server.js && node --check analyze.js && node --check analysis-engine.js && node --check google-visit-service.js && node --check public/admin.js && node --check public/slideshow.js"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
