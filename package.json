{
  "name": "remote-photo-slides2",
  "version": "1.0.0",
  "private": true,
  "description": "Local-network photo slideshow with visit-aware timeline locations",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "analyze": "node analyze.js",
    "relabel": "node analyze.js --labels-only",
    "retag-jeju": "node analyze.js --retag-jeju",
    "check": "node --check launcher.js && node --check server.js && node --check analyze.js && node --check analysis-engine.js && node --check google-visit-service.js && node --check public/admin.js && node --check public/slideshow.js"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
